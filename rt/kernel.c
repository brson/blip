#include "core.h"
#include <stdlib.h>

typedef struct ak_kernel_ ak_kernel;

struct ak_kernel_ {
};

ak_kernel *
ak_start() {
  abort();
}

int
ak_join(ak_kernel *kernel) {
  abort();
}

ac_address
ak_load_package(char *name) {
  abort();
}

